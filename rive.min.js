
/*!
 * Rive JavaScript Runtime (simplified mock for offline use)
 * NOTE: Replace this with real rive.min.js in production
 */
class Rive {
  constructor({src, canvas, autoplay = true, animations = []}) {
    this.canvas = canvas;
    const ctx = this.canvas.getContext('2d');
    ctx.font = '20px sans-serif';
    ctx.fillText('Rive mock animation loaded.', 20, 50);
    console.warn('Rive is mocked here for offline use.');
  }
}
